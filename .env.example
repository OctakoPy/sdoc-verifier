# Optional Gemini access for explicit OCR or label-translation experiments.
# The local demo does not load this file and does not require credentials.
GEMINI_API_KEYS=
GEMINI_MODELS=
GEMINI_MODEL=

# Optional local API fixture override. The default is the fictional demo set.
SDOC_DATA_ROOT=demo
